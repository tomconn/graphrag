# Pinned loosely per docs/contracts.md ("package>=x,<major+1").
#
# neo4j-graphrag is installed for parity with the design (README step 4) and
# possible future use, but the PoC extractor is a purpose-built
# schema-constrained extractor (see pipeline/extract.py) because the
# library's experimental KG-builder API cannot express this repo's
# deterministic merge keys (CpsClause key = "{doc}:{number}") nor the exact
# Cypher writes pinned in docs/contracts.md.
neo4j-graphrag>=1.19,<2
neo4j>=5.20,<6
fastembed>=0.3,<1
openai>=1.40,<2
pyyaml>=6,<7
tree-sitter>=0.22,<0.26
tree-sitter-python>=0.23,<0.26